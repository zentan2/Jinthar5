import React from 'react'
import api from '../api'
import Navbar from '../../components/Navbar'

const Portfolio = () => {
    return (
        <div
            style={{
                background: '#EEE',
                padding: '10%',
            }}>
            <Navbar/>
        </div>
    )
}

export default Portfolio